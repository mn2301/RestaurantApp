<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             xmlns:converters="clr-namespace:RestaurantApp.Converters"
             xmlns:local="clr-namespace:RestaurantApp"
             x:Class="RestaurantApp.History"
             Title="Mis pedidos"
             BackgroundColor="#131313">

    <Grid RowDefinitions="Auto, Auto" Padding="10">
        <!-- Header -->
        <VerticalStackLayout HorizontalOptions="Center" Grid.Row="0" Padding="20" Spacing="5">
            <Label Text="Historial de pedidos" TextColor="#E5E2E1" FontSize="28" FontFamily="ZalandoSans" FontAttributes="Bold"/>
        </VerticalStackLayout>

        <!-- Order grid (Syncfusion) -->
        <Border Grid.Row="1" BackgroundColor="#2A2A2A" HeightRequest="600" Stroke="#1C1B1B">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20" />
            </Border.StrokeShape>

            <syncfusion:SfDataGrid x:Name="orderGrid" HeightRequest="600" AutoGenerateColumnsMode="None" ColumnWidthMode="FitByCell"  HeaderRowHeight="50" RowHeight="80" SelectionMode="None" BackgroundColor="Transparent" HorizontalScrollBarVisibility="Always" VerticalScrollBarVisibility="Always">
                <syncfusion:SfDataGrid.DefaultStyle>
                    <syncfusion:DataGridStyle HeaderRowBackground="#1C1B1B" HeaderRowTextColor="#74A173" RowTextColor="#E5E2E1" HeaderRowFontSize="12" HeaderRowFontAttributes="Bold"/>
                </syncfusion:SfDataGrid.DefaultStyle>

                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:DataGridTemplateColumn HeaderText="FECHA" Width="100" HeaderTextAlignment="Center">
                        <syncfusion:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label Text="{Binding order.date, StringFormat='{}{0:MM/dd/yyyy a\\ h:mm tt}'}" TextColor="#E5E2E1" HorizontalOptions="Center" LineBreakMode="WordWrap" FontFamily="ZalandoSans" VerticalOptions="Center" FontSize="12"/>
                            </DataTemplate>
                        </syncfusion:DataGridTemplateColumn.CellTemplate>
                    </syncfusion:DataGridTemplateColumn>
                    
                    <syncfusion:DataGridTemplateColumn HeaderText="ESTADO" Width="100" HeaderTextAlignment="Center">
                        <syncfusion:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="10, 5" BackgroundColor="#2A2A2A" Stroke="#2A2A2A" VerticalOptions="Center" HorizontalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="28" />
                                    </Border.StrokeShape>
                                    <Label Text="{Binding order.status}" TextColor="#E5E2E1" LineBreakMode="WordWrap" FontSize="12" FontFamily="ZalandoSans" FontAttributes="Bold"/>
                                </Border>
                            </DataTemplate>
                        </syncfusion:DataGridTemplateColumn.CellTemplate>
                    </syncfusion:DataGridTemplateColumn>

                    <syncfusion:DataGridTemplateColumn HeaderText="ENTREGA" Width="100" HeaderTextAlignment="Center">
                        <syncfusion:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label Text="{Binding order.eatlocation}" LineBreakMode="WordWrap" HorizontalOptions="Center" TextColor="#E5E2E1" FontFamily="ZalandoSans" VerticalOptions="Center" FontSize="12"/>
                            </DataTemplate>
                        </syncfusion:DataGridTemplateColumn.CellTemplate>
                    </syncfusion:DataGridTemplateColumn>

                    <syncfusion:DataGridTemplateColumn HeaderText="$" Width="100" HeaderTextAlignment="Center">
                        <syncfusion:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Label Text="{Binding order.totalprice, StringFormat='{0:C2}'}" LineBreakMode="WordWrap" HorizontalOptions="Center" TextColor="#E5E2E1" FontFamily="ZalandoSans" VerticalOptions="Center" FontSize="12"/>
                            </DataTemplate>
                        </syncfusion:DataGridTemplateColumn.CellTemplate>
                    </syncfusion:DataGridTemplateColumn>

                    <syncfusion:DataGridTemplateColumn Width="50">
                        <syncfusion:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image x:Name="imgLink" Source="link.png" BackgroundColor="Transparent" WidthRequest="20" HeightRequest="20">
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="imgLink_Clicked"></TapGestureRecognizer>
                                    </Image.GestureRecognizers>
                                </Image>
                            </DataTemplate>
                        </syncfusion:DataGridTemplateColumn.CellTemplate>
                    </syncfusion:DataGridTemplateColumn>
                </syncfusion:SfDataGrid.Columns>
            </syncfusion:SfDataGrid>
        </Border>
    </Grid>
</ContentPage>